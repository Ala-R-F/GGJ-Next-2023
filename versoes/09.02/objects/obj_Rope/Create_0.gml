
global.parent = -1;

/*
ide = 0;

if (ide == 10){
	monster = instance_create_layer(x, y + offset, obj_Mons);
	conMons = physics_joint_distance_create(self, monster, self.x, self.y, monster.x, monster.y, false);
	with(monster){
		parent = other.next_rope;
	}
	physics_joint_set_value(conMons, phy_joint_damping_ratio, 1); //Elasticidade
	physics_joint_set_value(conMons, phy_joint_frequency, 10); //Frequência de update
}
*/